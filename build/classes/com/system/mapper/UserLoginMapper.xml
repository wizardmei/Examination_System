<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.mapper.UserLoginMapper">
  	<select id="selectByUserName" resultType="com.system.po.UserLogin">
  		select * from userlogin where username=#{username} and password=#{password}
  	</select>
  	<select id="findByName" resultType="com.system.po.UserLogin">
  		select * from userlogin where username=#{username}
  	</select>
  	<select id="findUser" resultType="com.system.po.UserLogin">
  		select * from userlogin where userid=${userid} and username='${username}' 
  	</select>
	<update id="resetUser">
		update userlogin set password='${passwd}',role=${role} where userid=${userid} and username='${username}'
	</update>
  	<update id="resetPasswd">
  		update userlogin set password='${newpasswd}' where username='${username}'
  	</update>
  	<insert id="createUser">
  		insert into userlogin(username,password,role) values('${username}','${passwd}',${role})
  	</insert>
  	<delete id="deleteUser">
  		delete from userlogin where username='${username}'
  	</delete>
 </mapper>